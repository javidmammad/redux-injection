import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        flex: 1,
        justifyContent: 'center',
        alignItems: 'center',
        backgroundColor: '#0A0A0A', //TODO should be in config
    },
    textLabel: {
        fontSize: 16,
        color: 'white', //TODO should be in config
    }
})